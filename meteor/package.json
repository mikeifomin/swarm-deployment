{
  "name": "meteor-jenkins",
  "version": "0.0.4",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "build":"meteor build --architecture os.linux.x86_64 ../.builds",
    "test": "meteor test --once --driver-package dispatch:mocha-phantomjs"
  },
  "dependencies": {
    "babel-runtime": "^6.18.0",
    "meteor-node-stubs": "~0.2.3"
  }
}
